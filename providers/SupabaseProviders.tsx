'use client';

import { FC, ReactNode } from 'react';

interface SupabaseProvidersProps {
  children: ReactNode;
}

const SupabaseProviders: FC<SupabaseProvidersProps> = ({}) => {
  return <div>SupabaseProviders</div>;
};

export default SupabaseProviders;
